{
  "name": "crisis-sync",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "start": "turbo run start",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
